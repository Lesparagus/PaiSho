Clone the repo into a directory
Run "pip install pygame-ce" in that directory.  This only needs to be done once, it installs the game framework
Start a game by running "python ./paisho.py" in that directory.
